# free-apk-builder (mobile-friendly)

Use Actions → Build Native APK from description → Run workflow → Enter app description.
After run completes, check the workflow run and download the artifact `app-debug-apk` (APK).
